    //define one observable named "NEvts" with range [0, 2000] in 1 bin.
    observables = {
	 NEvts = {
         	range = (0.0, 2000.0);
		nbins = 1;	
	 };
    };

    //the parameters of the model are the number of signal and
    // background events after the selection
    parameters = {
        s={
            default = 150.0;
            range = (0.0, "inf");
        };
        b_1={
            default = 10.0;
            range = (0.0, "inf");
        };
        b_2={
            default = 80.0;
            range = (0.0, "inf");
        };
    };

countingExp = {
    //specification of the "NEvts" observable
    NEvts = {
        signal = { // "signal" is a name you can choose freely here
            coefficients = ("s");
            histogram = {
                type = "fixed-poly";
                observable = "NEvts";
                normalize_to = 1.0;
		coefficients = [1.0];
            };
        };
        
        bkg_1 = {
            coefficients = ("b_1");
            histogram = {
                type = "fixed-poly";
                observable = "NEvts";
                normalize_to = 1.0;
                coefficients = [1.0];
            };
        };

        bkg_2 = {
            coefficients = ("b_2");
            histogram = {
                type = "fixed-poly";
                observable = "NEvts";
                normalize_to = 1.0;
                coefficients = [1.0];
            };
        };

    };

    constraints = {
        c_bkg_1 = { // the name "c_bkg_1" can be chosen at will
            type = "gauss";
            parameter = "b_1";
            mean  = 10.0;
            width =  5.0;
        };
        c_bkg_2 = {
            type = "gauss";
            parameter = "b_2";
            mean  = 80.0;
            width = 20.0;
        };
    };

};

plugins = {
  files = ("lib/core-plugins.so", "lib/root.so");
};

