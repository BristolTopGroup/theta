@include "gaussoverflat-include.cfg"

scan = {
   type = "nll_scan";
   parameter = "s";
   minimizer = { type = "root_minuit"; };
   parameter-values = {start = 0.0; stop = 1.0; n-steps = 101;};
};

main = {
   type = "plain_run";
   model = "@gaussoverflat";
   producers = ("@scan");
   n-events = 10;
   result-file = "gaussoverflat-scan.db";
};
